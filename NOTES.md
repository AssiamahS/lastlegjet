- 2026-07-27: exportArchive "Cloud signing permission error / No profiles for X" with 8RF7P8XRQ9 ASC key — that key can't do cloud-managed DISTRIBUTION signing; KB93R49B9J can. Archive (dev signing) works with either, so the failure only shows at export. Diagnosed via GH secret-masking: successful runs show "Created via API (***)" in the cert name.
- 2026-08-07: App Store submission via ASC API, learned: Mac clock 25h behind → mint JWT from a real HTTP Date header, not local time. ageRatingDeclaration hangs off /v1/appInfos/{id} now (not appStoreVersions). appPriceSchedules inline appPrices need literal '${p0}' local-id syntax. App Privacy answers have NO API anymore (old appDataUsages removed) — UI-only, it's the ONLY submit blocker left. Open reviewSubmission 631d15b0-7c22-4456-b02c-7bf721e8a362, version 70f530bb-c6b6-4ef4-b52b-e88e21b3d311, build 8 attached; after user publishes App Privacy: POST reviewSubmissionItems + PATCH submitted=true.
- 2026-08-21: App rebuilt as the Last Leg Jet marketplace client (v2.0.0) against the NestJS API in ~/lastlegjet-marketplace; bundled sample JSON in ios/Resources/SampleData makes the whole booking flow demoable offline. deploymentTarget iOS 26 means the simulator build job must also run on macos-26 (Xcode 16 on macos-15 has no iOS 26 SDK).
